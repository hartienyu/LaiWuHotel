page {
  background-color: #f6f6f6;
  box-sizing: border-box;
}

.discover-container {
  /* 如果您有自定义顶部导航栏，保留这个 padding-top；如果没有，可以改小一点 */
  padding-top: 180rpx; 
  padding-bottom: env(safe-area-inset-bottom);
  padding-left: 24rpx;
  padding-right: 24rpx;
}

/* --- 🟢 原有的 Header 样式 --- */
.header {
  /* background: #fff;  <-- 建议去掉这个背景色，让它透出底色，或者保留看您喜好 */
  padding: 10rpx 8rpx; /* 稍微调整了内边距 */
  margin-bottom: 24rpx;
  display: flex;
  flex-direction: column;
}

.title {
  font-size: 44rpx; /* 稍微加大一点，更有冲击力 */
  font-weight: 800; /* 加粗 */
  color: #333;
  margin-bottom: 12rpx;
}

.subtitle {
  font-size: 26rpx;
  color: #666;
}

/* --- 🟢 新增的酒店列表样式 --- */

/* 酒店卡片主体 */
.hotel-card {
  background: #ffffff;
  border-radius: 24rpx; /* 圆角更大一点 */
  overflow: hidden;
  margin-bottom: 30rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.03); /* 增加一点阴影质感 */
}

/* 酒店大图 */
.hotel-cover {
  width: 100%;
  height: 320rpx; /* 图片高度增加一点 */
  display: block;
}

.hotel-info {
  padding: 24rpx;
}

/* 标题行 */
.name-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.hotel-name {
  font-size: 34rpx;
  font-weight: bold;
  color: #333;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  margin-right: 16rpx;
}

/* 评分胶囊 */
.score-badge {
  background: #fff8f0;
  padding: 6rpx 14rpx;
  border-radius: 12rpx;
  color: #fa4126;
  font-weight: bold;
  flex-shrink: 0;
}

.score-num {
  font-size: 28rpx;
}

.score-unit {
  font-size: 20rpx;
  margin-left: 2rpx;
}

/* 标签行 */
.tags-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.tag {
  font-size: 20rpx;
  color: #666;
  background: #f5f5f5;
  padding: 6rpx 12rpx;
  border-radius: 6rpx;
}

.divider {
  height: 1rpx;
  background: #f0f0f0;
  margin-bottom: 24rpx;
}

/* 房型列表 */
.room-item {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
}

.room-item:last-child {
  margin-bottom: 0;
}

.room-thumb {
  width: 110rpx;
  height: 110rpx;
  border-radius: 12rpx;
  margin-right: 20rpx;
  background: #eee;
  flex-shrink: 0;
}

.room-detail {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.room-name {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 8rpx;
  font-weight: 500;
}

.room-info {
  font-size: 22rpx;
  color: #999;
}

.room-price {
  color: #fa4126;
  font-weight: bold;
  flex-shrink: 0;
}

.symbol {
  font-size: 22rpx;
  margin-right: 2rpx;
}

.num {
  font-size: 34rpx;
}